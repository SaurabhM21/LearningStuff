/* 
 * Algorithm: Insertion Sort
 * 
 */

function insertionSort(sortInput) {
  var inputArray = '';
  var inputArrayLength = 0;

  if (sortInput !== null || sortInput !== undefined) {
    inputArray = sortInput.split(',');
    inputArrayLength = inputArray.length;

    for (var idx=0; idx < inputArrayLength; idx++) {
      
    }
  }

}


insertionSort("5, 2, 4, 6, 1, 3")

